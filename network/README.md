# Network configuration

NetworkManager profile source and generation rules live in
`NetworkManager/`. No profile in this repository contains a usable credential,
and none is installed during local development.

NetworkManager access-point configuration will live here. It is noncritical to recording and
must never include a universal or committed device password.
