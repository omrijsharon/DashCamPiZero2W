# DashCam Pi Zero 2 W implementation plan

Last updated: 2026-07-28

This is the execution checklist for `Pizero_dashcam_PROJECT.md`. The product specification is the acceptance contract; this plan orders the work and records verified progress.

## Checklist rules

- Check a task (`[x]`) only after its validation is complete and the evidence is saved in the repository or linked test report.
- Check a milestone only after every task beneath it, including its exit gate, is checked.
- Local fixtures and mocks can prove local logic, but they cannot prove Pi hardware, performance, provisioning, power-loss, or Windows compatibility.
- Keep target-dependent decisions provisional until measured on the exact Raspberry Pi OS image and Pi Zero 2 W.
- Record deviations in `docs/architecture.md` or a short architecture-decision record with the reason, evidence, risk, and remedy.
- Tags: **LOCAL** = development machine; **PI** = Pi required; **WIN** = Windows host required; **DESTRUCTIVE** = expendable-card/destructive operation.

## Milestone 0 — Specification approval and implementation authorization

- [x] **Milestone 0 complete**
  - [x] **LOCAL:** Review the full product specification for feasibility, contradictions, and missing reliability constraints.
  - [x] **LOCAL:** Correct `Pizero_dashcam_PROJECT.md` without writing implementation code.
  - [x] **LOCAL:** Create this checkbox-based implementation plan.
  - [x] **LOCAL:** Create repository-specific `AGENTS.md`.
  - [x] Owner reviews the revised specification and plan.
  - [x] Owner explicitly authorizes local implementation to begin (2026-07-24).
  - [x] Exit gate: implementation scope and unresolved product choices are accepted as provisional pending the later Pi capability gate.

## Milestone 1 — Local repository foundation

- [x] **Milestone 1 complete**
  - [x] **LOCAL:** Add `README.md`, license decision, Python/package layout, and supported Python version.
  - [x] **LOCAL:** Add `pyproject.toml` with pinned or bounded runtime/development dependencies.
  - [x] **LOCAL:** Configure formatter, linter, type checker, and unit-test runner.
  - [x] **LOCAL:** Add CI for formatting, linting, type checks, unit tests, and artifact retention.
  - [x] **LOCAL:** Create the planned source, test, config, script, deploy, systemd, network, and docs directories.
  - [x] **LOCAL:** Add application/build version reporting and a reproducible version source.
  - [x] **LOCAL:** Define machine-readable test-result and capability-report formats.
  - [x] **LOCAL:** Add bounded structured logging conventions with secret redaction.
  - [x] **LOCAL:** Document the development workflow and which tests require Pi/Windows hardware.
  - [x] **LOCAL:** Run the complete local quality suite from a fresh isolated source copy; a literal Git checkout awaits the initial commit.
  - [x] Exit gate: the minimal package imports/builds, the CI-equivalent suite passes, and no target-hardware capability is claimed. Evidence: `docs/test-reports/2026-07-24-milestone1-local.md`.

## Milestone 2 — Local domain model and hardware-independent logic

- [x] **Milestone 2 complete**
  - [x] **LOCAL:** Define versioned TOML configuration models, defaults, validation, atomic update, and migration behavior.
  - [x] **LOCAL:** Keep AP/session secrets outside world-readable configuration and redact them from reads/logs.
  - [x] **LOCAL:** Define separate recorder, storage, time, GPS, audio, and device-operation state models.
  - [x] **LOCAL:** Define clip lifecycle, orthogonal protection flag, bounded download lease, and stable UUID clip identity.
  - [x] **LOCAL:** Implement Windows-safe provisional/final filename generation with boot-ID and sequence collision protection.
  - [x] **LOCAL:** Define the versioned sidecar schema, including nullable UTC fields and time-anchor provenance.
  - [x] **LOCAL:** Build NMEA fixtures for valid, bad-checksum, malformed, mixed-talker, stale, invalid-fix, ZDA, RMC, and GGA cases.
  - [x] **LOCAL:** Implement/test sentence-specific NMEA trust rules and bounded input handling.
  - [x] **LOCAL:** Implement/test monotonic-to-UTC anchoring, plausibility checks, conflict handling, and IANA timezone conversion.
  - [x] **LOCAL:** Unit-test `Asia/Jerusalem` around both daylight-saving transitions, UTC midnight, and local-date rollover.
  - [x] **LOCAL:** Implement/test pure retention selection and low/high/emergency threshold calculations.
  - [x] **LOCAL:** Model durable intents and idempotent reconciliation for finalize, rename, protect/unprotect, and pair deletion.
  - [x] **LOCAL:** Define versioned public/internal API schemas, stable error codes, and authentication/CSRF expectations.
  - [x] **LOCAL:** Add property/fuzz tests for filenames, paths, configuration boundaries, NMEA input, and state transitions.
  - [x] Exit gate: all hardware-independent logic passes locally with bounded-input tests and no direct device access. Evidence: `docs/test-reports/2026-07-24-milestone2-local.md`.

## Milestone 3 — Local deployment, diagnostics, and test harnesses

- [x] **Milestone 3 complete**
  - [x] **LOCAL:** Author a read-only Pi capability probe covering OS/kernel, architecture, memory, camera, encoders, media stacks, audio, UART, filesystems, thermals, throttling, and undervoltage.
  - [x] **LOCAL:** Make the probe report raw evidence plus a machine-readable summary; it must not alter the Pi.
  - [x] **LOCAL:** Author media validation tooling for streams, codecs, decodability, IDR starts, duration, normalized boundary continuity, bitrate, and A/V skew.
  - [x] **LOCAL:** Author endurance monitoring for RSS, available memory, swap, CPU, temperature, throttling, frame drops, bitrate, and service restarts.
  - [x] **LOCAL:** Draft a declarative partition layout and a non-destructive layout verifier.
  - [x] **LOCAL:** Draft provisioning logic with dry-run output, device-identity invariants, idempotency markers, and explicit refusal cases.
  - [x] **LOCAL:** Draft installation, upgrade, rollback, recovery, and log-collection commands.
  - [x] **LOCAL:** Draft `systemd` units with ordering, restart backoff, watchdog notification, privilege restrictions, and bounded shutdown.
  - [x] **LOCAL:** Draft NetworkManager AP configuration without a universal password.
  - [x] **LOCAL:** Add scripts/test instructions for power-loss recovery, small-filesystem retention, and Windows interop.
  - [x] **LOCAL:** Review every destructive command path and ensure dry-run/refusal behavior is testable without a block device.
  - [x] Exit gate: tools are locally tested against fixtures; destructive tooling has not been run and hardware results remain unknown. Evidence: `docs/test-reports/2026-07-24-milestone3-local.md`.

## Milestone 4 — Pi access and capability decision gate

- [x] **Milestone 4 complete**
  - [x] Obtain owner authorization for Pi access and the connection details; do not SSH before this task is approved. Authorized 2026-07-24; evidence: `docs/test-reports/2026-07-24-milestone4-progress.md`.
  - [x] **PI:** Record Pi model/revision, camera model, GPS model, microphone identity, card model/capacity, power supply, and power controller. Reference supply is an unspecified-model regulated 5 V / 2.5 A unit; no hold-up/shutdown controller exists. Evidence: `docs/test-reports/2026-07-24-milestone4-progress.md` and `docs/test-reports/2026-07-24-pi-gps-audio.md`.
  - [x] **PI:** Record the exact Raspberry Pi OS Lite image/release date, 32/64-bit architecture, kernel, packages, and boot configuration. Evidence: `docs/test-reports/2026-07-24-milestone4-progress.md`.
  - [x] **PI:** Run the read-only capability probe and save its full report. Evidence: `docs/test-reports/2026-07-24-pi-capability-post-uart.json`.
  - [x] **PI:** Compare 32-bit and 64-bit image memory/media compatibility if the release choice is not already proven. The exact 32-bit image proved the required media path with resource margin; re-open only if a dependency or endurance result invalidates it.
  - [x] **PI:** Probe camera modes, raw/ISP stream formats, buffer paths, and hardware H.264 encoder controls. Evidence: `docs/test-reports/2026-07-24-milestone4-progress.md`.
  - [x] **PI:** Prove 1920×1080 at 30 fps hardware H.264 at the target bitrate with no software fallback. Evidence: direct `/dev/video11` ownership, PTS, and validated MP4 artifacts in `docs/test-reports/2026-07-24-milestone4-progress.md`.
  - [x] **PI:** Probe SPS/PPS repetition, IDR request/interval control, supported H.264 profiles/levels, and closed-GOP behavior. Evidence: controls plus IDR-starting fragmented segments in `docs/test-reports/2026-07-24-milestone4-progress.md`.
  - [x] **PI:** Compare viable GStreamer, Picamera2, and/or `rpicam` integration paths and record the selected camera owner. Selected one in-process GStreamer graph owned by `dashcamd`; evidence: `docs/architecture.md`.
  - [x] **PI:** Compare standard MP4, fragmented MP4, and robust muxing/finalization behavior. Evidence: `docs/test-reports/2026-07-24-pi-mux-recovery-validation.json`.
  - [x] **PI:** Verify USB audio stable identity, formats/rates, disconnect behavior, and USB power/topology. Identity, native formats, direct-root topology, PCM/AAC capture, bounded absent-device failure, subsequent video-only capture, and reconnect identity/AAC capture passed. Evidence: `docs/test-reports/2026-07-24-pi-gps-audio.md`.
  - [x] **PI:** Resolve `/dev/serial0`; validate fixed-clock mini UART or deliberate PL011 remapping and document Bluetooth consequences. Selected `/dev/ttyAMA0` PL011 with Bluetooth disabled; evidence: `docs/test-reports/2026-07-24-milestone4-progress.md`.
  - [x] **PI:** Measure baseline filesystem throughput, CPU, memory, temperature, throttling, and undervoltage. The non-destructive baseline is recorded; exFAT write/endurance measurements remain later storage gates.
  - [x] **PI:** Probe overlay-capable formats and preview stream/encoder candidates without starting a second camera owner. Evidence: NV12 overlay and same-`libcamerasrc` dual-stream measurements in `docs/test-reports/2026-07-24-milestone4-progress.md`.
  - [x] **PI:** Record architecture decisions, rejected alternatives, raw measurements, and remaining risks. Evidence: `docs/architecture.md` and `docs/test-reports/2026-07-24-milestone4-progress.md`.
  - [x] Exit gate: the exact production media path, OS architecture, UART configuration, GPS baud/protocol/UTC, USB-audio path and capability fault behavior, candidate muxer, and reference supply capacity are evidence-backed.

## Milestone 5 — SSH-first development deployment, recording-volume provisioning, and mount safety

- [ ] **Milestone 5 complete**
  - **Active route (2026-07-25):** SSH-first development deployment. Use an official Raspberry Pi OS Lite 32-bit Trixie image customized by Raspberry Pi Imager; do not build or flash a custom Bootstrap image for current Pi development.
  - [x] **WIN/PI:** Flash the official Raspberry Pi OS Lite 32-bit Trixie image for the declared hostname, user, home Wi-Fi, and SSH public key, using only the authorized card.
  - [x] **WIN/DESTRUCTIVE:** Before that image's first Pi boot, remove exactly one standalone stock `resize` token from FAT `cmdline.txt`, preserve every other token/byte, verify zero remaining standalone `resize` tokens, preserve the partition table, and flush the Windows volume cache.
  - [x] **PI/READ-ONLY:** Discover the first boot on the configured network and make the first SSH login. Save OS, boot ID, card CID, disk, partition, free-space, and network evidence; do not install large dependencies or mutate storage yet. Passed; evidence: `docs/test-reports/2026-07-25-ssh-first-boot.md`.
  - [x] **PI/READ-ONLY:** Reconfirm official Raspberry Pi OS Lite 32-bit Trixie and the mounted stock root as p2 with exactly 4,161,536 sectors before accepting provisioning. Passed; evidence: `docs/test-reports/2026-07-25-ssh-first-boot.md`.
  - [x] **LOCAL:** Adapt the Stage A planner/executor for SSH-first provisioning: grow stock p2 directly to exactly 6 GiB and create p3 from the remainder, retaining exact CID/layout gating, MBR backup/hash, durable intent, one `sfdisk --no-reread` write, raw-MBR readback/commit, sync, and exactly one controlled reboot. Passed; evidence: `tests/unit/test_bootstrap_stock_dev.py` and `docs/test-reports/2026-07-25-ssh-first-stock-provisioner-local.md`.
  - [x] **LOCAL:** Adapt Stage B for SSH-first provisioning: require a different boot ID and exact revalidation; grow mounted ext4 online with `resize2fs`; bind the stock future-p3 4 MiB prefix hash before Stage A and require the identical hash plus no recognized filesystem/signature before format intent; format p3 once as exFAT `DASHCAM`; configure UUID mounting at `/srv/dashcam`, sentinel/directories/environment, and write completion last. Passed; evidence: `tests/unit/test_bootstrap_stock_dev.py` and `docs/test-reports/2026-07-25-ssh-first-stock-provisioner-local.md`.
  - [x] **LOCAL:** Add/update focused tests and a true planner-only live `--dry-run` for the exact stock-p2-to-6-GiB SSH-first contract, including refusal for mismatched CID, layout, root disk, target size, prefix-hash drift, trigger confusion, and non-terminal cloud-init. `ready=false` exits nonzero and cannot invoke mutators. Passed; evidence: `tests/unit/test_bootstrap_stock_dev.py` and `docs/test-reports/2026-07-25-ssh-first-stock-provisioner-local.md`.
  - [x] **LOCAL:** Package the minimal SSH-development payload: reviewed schema-2 exact-card contract, provisioner module, boot-marker arming/readback helper, strict minimal account/directory installer, closed manifest, and bounded invocation/evidence commands. Do not include or install the full application dependency set. Passed; 113,593-byte payload and manifest evidence: `docs/test-reports/2026-07-25-ssh-first-stock-provisioner-local.md`.
  - [x] **PI/READ-ONLY:** Transfer only that reviewed minimal provisioning payload to tmpfs; verify its closed allowlist, 113,593-byte total, and every hash on the Pi without installing packages or enabling an automatic destructive service. Passed; remote manifest SHA-256 `1ee510e58ceba23ef780cce46fe9c0ad71651d2ad96fc1a034ca149721e2b0a8`.
  - [x] **PI/INSTALL:** Run the manifest-gated minimal installer only. Verify exact local service accounts, strict directories, installed file hashes/modes, unchanged cmdline and partition table, absent provisioning journal, and no enabled service/package/storage action. Passed twice idempotently; cmdline SHA-256 `a57c536af29922659a75115002770c69d5a82dbc151ac580a2629c2769e7aa1f`, MBR SHA-256 `2487b1f9af5151759ad1ec762d077424736d38d01a68b72b8d6d4e1634545c3a`.
  - [x] **PI/BOOT-CONFIG:** Add exactly one `dashcam.bootstrap=ssh-dev-v1` token to FAT `cmdline.txt`, prove the stock `resize` token remains absent and every unrelated token is preserved, flush, and perform one preparatory reboot. Passed: cmdline changed from SHA-256 `a57c536af29922659a75115002770c69d5a82dbc151ac580a2629c2769e7aa1f` to `27e2887fe33dfae7ddb3c705e7b2bccdb895cbdd8e0eafac23b5e2ee0fd08d7d`; backup/readback passed; boot ID changed to `cf1614e7-35cb-42e1-9223-f2473dd80978`; `/proc/cmdline` has exactly one development marker and no release/resize token.
  - [x] **PI/READ-ONLY:** Run the true exact-card/layout Stage A `--dry-run` immediately before the authorized transaction and save its `ready=true` JSON. Passed with exact CID, stock p2, stable future-p3 prefix hash, known terminal cloud-init state, and approved target; evidence: `docs/test-reports/2026-07-25-ssh-first-stage-a-dry-run.json`.
  - [x] **PI/DESTRUCTIVE:** Execute Stage A once on the exact authorized card, capture raw-MBR readback/durable-commit evidence, and allow exactly one controlled reboot. Do not install the full repository or large dependencies first. Passed; evidence: `docs/test-reports/2026-07-25-ssh-first-stage-a-b-progress.md`.
  - [x] **LOCAL/PI RECOVERY:** Correct the ext4 total-geometry observer exposed by the live Stage B attempt, add the exact-value regression, validate locally, install only the reviewed fix, preserve the false-refusal journal, and clear only that exact refusal through an identity-bound reviewed recovery. Leave unrelated or mismatched refusals latched. Passed; evidence: `docs/test-reports/2026-07-25-ssh-first-stage-a-b-progress.md`.
  - [x] **LOCAL/PI RECOVERY:** Correct post-format reconciliation for the exact normal exFAT `wipefs` signature pair (`exfat` at `0x3`, `dos` at `0x1fe`) without weakening pre-format blankness, preserve the exact false-refusal journal, and restore only `FORMAT_INTENT` through a second identity-bound helper. Prove the normal state machine does not reformat. Passed; evidence: `docs/test-reports/2026-07-25-ssh-first-stage-a-b-progress.md`.
  - [x] **PI/DESTRUCTIVE:** On the different boot ID, complete Stage B once: revalidate identity/layout, prove ext4 is exactly 6 GiB, require the journal-bound future-p3 prefix hash to remain identical with no recognized filesystem/signature, record format intent, format only p3 as exFAT `DASHCAM`, mount it by UUID at `/srv/dashcam`, create the sentinel, and write completion last. Preserve evidence. Passed with two exact, audited state-only recoveries for observer defects; exFAT was formatted once and the repeated completed invocation was a byte-identical no-op. Evidence: `docs/test-reports/2026-07-25-ssh-first-stage-a-b-progress.md`.
  - [x] **PI:** Verify partition-table backup, alignment, UUID capture, sentinel creation, completion-marker-last semantics, and idempotent later invocations. Passed; evidence: `docs/test-reports/2026-07-25-ssh-first-stage-a-b-progress.md`.
  - [x] **PI:** Mount `/srv/dashcam` by UUID with validated exFAT options and service-account access. Passed with UUID `7EED-3EA7`; evidence: `docs/test-reports/2026-07-25-ssh-first-stage-a-b-progress.md`.
  - [x] **LOCAL:** Create and validate one repeatable, idempotent SSH-development application installer and closed bundle for the locked production Python environment, required APT/media dependencies, configuration, and reviewed systemd artifacts. Passed after real-Windows short-read, ownership, Trixie APT-solver, stock `os-release`, and sysfs-CID observer corrections; evidence: `docs/test-reports/2026-07-26-ssh-first-app-install.md`.
  - [x] **LOCAL:** Keep recorder/web/prepare-removal units disabled until their referenced production entry points and watchdog/shutdown contracts exist. Validate installability of only implemented entry points; do not represent the current unit drafts as runnable. Passed: the live installer refuses those unit paths, enables only storage preflight, and starts no service; evidence: `docs/test-reports/2026-07-26-ssh-first-app-install.md`.
  - [x] **PI:** Transfer the complete reviewed working tree and closed application bundle to a fresh unprivileged staging directory, excluding `.git`, virtual environments, caches, `artifacts`, build outputs, and bytecode; verify archive and manifest hashes before installation. Passed with a 297-file working-tree archive and closed bundle; evidence: `docs/test-reports/2026-07-26-ssh-first-app-install.md`.
  - [x] **PI:** Run the application installer idempotently, preserve installed package/version evidence, and verify rootfs retains at least 2 GiB free afterward. Adjust the target size only through a reviewed contract change. Passed twice; explicit APT index refresh occurred before the replacement authoritative dry-run and never between that plan and apply; final root free 3,622,191,104 bytes; evidence: `docs/test-reports/2026-07-26-ssh-first-app-install.md`.
  - [x] **PI:** Validate the installed storage preflight and other implemented non-recorder entry points without a microphone. Defer recorder/web service startup to their implementation milestones and USB-microphone functional/A/V gates until the microphone is connected. Passed: storage check ran as `dashcam`, reached `READY`, and completed its writable probe; other production entry points remain intentionally absent; evidence: `docs/test-reports/2026-07-26-ssh-first-app-install.md`.
  - [x] **PI:** Prove recorder preflight rejects an unmounted directory, wrong filesystem, wrong label/UUID/sentinel, read-only volume, and rootfs fallback while NetworkManager and SSH remain usable. Passed all six exact-reason cases in private mount namespaces with no write probe, unchanged real storage, unchanged network namespace, and a live SSH banner after every case; evidence: `docs/test-reports/2026-07-26-milestone5-live-validation.md`.
  - [x] **PI:** Test bounded `fsck.exfat` behavior for clean, dirty/repairable, and failed/unrepairable volumes; prove no auto-format. Passed on disposable loop images: clean stayed byte-identical, corrupt primary boot checksum was restored from the intact backup and rechecked clean, invalid root metadata refused byte-identically, and no fsck case invoked a formatter; evidence: `docs/test-reports/2026-07-26-milestone5-live-validation.md`.
  - [x] **PI:** Measure sustained and burst write/finalization performance with reserve space enforced. Passed: 512 MiB at 17.78 MB/s, eight 16 MiB bursts, 26.519–37.915 ms finalization, 3,669,963,571-byte reserve preserved, and exact cleanup; evidence: `docs/test-reports/2026-07-26-milestone5-live-validation.md`.
  - [x] **PI:** Validate IMX219/NV12 camera capability, a bounded independently decodable Raspberry Pi hardware-H.264 sample, receive-only GPS UART/module/parser communication, and bounded home-Wi-Fi/local-route behavior on this image without treating microphone absence as a failure. Passed those scoped gates; the initial GPS window had no navigation fix but the open-sky task below later passed, while the GStreamer V4L2 encoder failed separately; evidence: `docs/test-reports/2026-07-26-milestone5-live-validation.md`.
  - [x] **PI:** Acquire a valid live GPS navigation fix and trusted RMC/ZDA UTC anchor on the exact final image. Passed after moving the antenna into the open: the privacy-safe 120-second run recorded 6–7 satellites and 1,153 trusted active-RMC anchors; an independent 15-second repository-parser check accepted 150/150 RMC anchors with seven satellites and 0.032–0.037-second system-minus-GPS observations. No coordinates were retained; evidence: `docs/test-reports/2026-07-26-milestone5-live-validation.md`.
  - [x] **PI:** Validate the connected USB microphone's stable identity, native capture caps, non-silent PCM signal, and standalone production AAC branch on the exact final image. Passed with dynamic `08bb:2902`/physical-path selection, 3.000-second 48 kHz mono S16_LE capture, AAC-LC at 127,997 bit/s, independent decode, non-silent/unclipped signal, exact tmpfs cleanup, and no service/network/throttle disturbance. The quiet-ambient run used the current minimum capture-gain step with AGC enabled; calibrated input gain and integrated A/V synchronization remained later tasks; evidence: `docs/test-reports/2026-07-26-milestone5-live-validation.md`.
  - [x] **PI:** Install the reviewed current-path fallback unit without activating AP and prove the home-Wi-Fi/client path remains bounded and usable. Passed: the hash-closed installer enabled the unit without starting it, a second apply was byte-idempotent, and the installed client path returned in 1.151 seconds with the same connection/address, no AP artifacts, and NetworkManager/SSH active; evidence: `docs/test-reports/2026-07-26-milestone5-live-validation.md`.
  - [ ] **OWNER-DEFERRED (2026-07-26) — PI/USER-ASSISTED:** Deliberately remove home connectivity and validate the stable `192.168.50.1/24` AP, credentials handoff, reconnection instructions, no oscillation, and SSH recovery with the owner present. The owner chose to return to this later; do not initiate it until explicitly resumed. Keep Milestone 5 and its exit gate open, but allow Milestone 6 implementation to proceed. Evidence so far: `docs/test-reports/2026-07-26-milestone5-live-validation.md`.
  - **DEFERRED RELEASE ACCEPTANCE:** Repeat provisioning and acceptance on a nominal 64 GB card and each later explicitly authorized capacity class; this does not block the exact-card SSH-first development gate.
  - [x] **DESTRUCTIVE AUTHORIZATION:** The owner authorized complete erase, reflash, repartition, and format only for the 31,457,280,000-byte card with CID `fe34325344000000200000031a0192d1`; general-release destructive authorization remains unresolved. Evidence: `docs/test-reports/2026-07-24-milestone5-progress.md`.
  - [x] **HISTORICAL/RETIRED:** The v1-v4 initramfs-based image experiments, their local evidence, and v2/v3 forensic captures are retained as historical evidence only. They are not Bootstrap v1 validation and must not be flashed again.
  - [x] **HISTORICAL/RETIRED:** V4 was flashed and powered, then observed for more than ten minutes with no expected MAC, hostname, IP, or SSH availability. No SSH session or laptop-initiated Pi/storage mutation occurred during that observation. Its post-boot card state was not captured, so no partition result or exact failure cause is claimed. The architecture is retired and its raw artifacts were deleted by the owner.
  - **DEFERRED RELEASE ENGINEERING:** The Bootstrap v1 custom-image build contract, preinstalled payload, temporary raw build image, retained `.img.xz`, and Imager manifest/hashes are retained work, not an active development prerequisite.
  - [x] **LOCAL:** Implement and test cmdline customization that removes exactly one standalone stock `resize` token, adds `dashcam.bootstrap=v1`, preserves Imager first-run tokens, and rejects ambiguous/missing token layouts. Evidence: `docs/test-reports/2026-07-25-bootstrap-v1-local.md`.
  - [x] **LOCAL:** Implement/test normal-post-root service ordering: preserve Imager `cloudinit-rpi`, order after `cloud-final.service`, require terminal successful cloud-init completion, retain defensive legacy-firstrun deferral, run storage independently of network, precede storage verification/dashcam writes, and become a no-op only after verified completion. Evidence: `docs/test-reports/2026-07-25-bootstrap-v1-local.md`.
  - [x] **LOCAL:** Implement/test Stage A planner and executor: derive mounted root/back disk, exact CID/layout gate, ext4+FAT MBR backup/hash, durable intent, one `sfdisk --no-reread` target write, raw MBR readback/commit, sync, and exactly one controlled reboot. Model/fake-runtime evidence: `docs/test-reports/2026-07-25-bootstrap-v1-local.md`; exact-card execution remains below.
  - [x] **LOCAL:** Implement/test Stage B: require a different boot ID and exact revalidation; run online `resize2fs` only and re-read its exact size; require newly-created p3 provenance, no known signatures, and the image-authored all-zero 4 MiB prefix before format intent; format once as exFAT `DASHCAM`; persist UUID mount/sentinel/directories/fstab/environment; completion marker last. Model/fake-runtime evidence: `docs/test-reports/2026-07-25-bootstrap-v1-local.md`; exact-card execution remains below.
  - [x] **LOCAL:** Add fault-injection/reconciliation tests for every Stage A/B boundary and power cut. Foreign, torn, conflicting, or destructive-refusal states must latch without auto-restore, auto-format, or destructive retry. Evidence: `tests/unit/test_bootstrap_storage_fault_matrix_v1.py` and `docs/test-reports/2026-07-25-bootstrap-v1-local.md`.
  - [x] **LOCAL:** Implement/test every-boot NetworkManager policy: bounded 60-second home-Wi-Fi association/local-route attempt, stable `Dashcam-<shortid>` AP fallback at `192.168.50.1/24` with unique WPA secret, and explicit retry/no oscillation. Network/AP failure must not block storage fault reporting or recording once storage is valid. Evidence: `docs/test-reports/2026-07-25-bootstrap-v1-local.md`.
  - **DEFERRED RELEASE ENGINEERING:** Resolve/pin the Linux builder and its qemu/binfmt/chroot/package contracts, build a locked wheelhouse, independently verify image inventories/provenance, and build/read back a Bootstrap `.img.xz`. Do not do this before the SSH-first Pi implementation and hardware gates pass.
  - [ ] Exit gate: SSH-first provisioning is safe/idempotent on the authorized card, recording cannot target the underlying rootfs directory, the full repository has a repeatable Pi installation path, and final-image standalone microphone capability is evidence-backed; integrated A/V and microphone-absent startup behavior were explicitly assigned to Milestone 7.

## Milestone 6 — Reliable video-only recorder

- [x] **Milestone 6 complete** — Exit evidence: `docs/test-reports/2026-07-27-milestone6-metrics-recovery-endurance-live.md`.
  - **Parallel-progress note:** Milestone 6 completed while the sole owner-assisted Milestone 5 AP activation test remained explicitly deferred.
  - [x] **LOCAL:** Implement the recorder daemon skeleton, configuration load, state reporting, cancellation, and `systemd` notification. Evidence: `docs/test-reports/2026-07-24-pre-pi-implementation.md`.
  - [x] **LOCAL:** Implement the storage-gated GStreamer recorder composition: exact IMX219/NV12-to-hardware-H.264 caps, bounded fragmented `splitmuxsink` output under `pending`, first-fragment readiness, continuous fragment-event draining, collision refusal, single process-local camera ownership, and cancellation-safe startup/shutdown. Local evidence: `docs/test-reports/2026-07-26-milestone6-recorder-local.md`; the separately checked target tasks below carry live evidence.
  - [x] **LOCAL:** Integrate crash-recoverable clip finalization: remove the active `.partial` suffix, write/read back a canonical bounded sidecar, durably register the ext4 `FINALIZE` intent before exFAT pair moves, use device-bound atomic no-replace promotion with directory flushes, supervise finalizer failures, await the final shutdown fragment, and replay only bounded durable intents at startup. MP4-only diagnostics remain untouched. Evidence: `docs/test-reports/2026-07-26-milestone6-finalization-local.md`.
  - [x] **PI:** Implement the selected single-owner continuous camera and hardware H.264 pipeline. The installed production runtime passed exact IMX219/NV12 negotiation, dynamically selected `v4l2h264enc`, 1920×1080/30 High/4.1, 8 Mb/s target, GOP 30, repeated headers, default hardware VBR, one-minute rotation, clean systemd shutdown, and independent decode. Never set `video_bitrate_mode=1`; that control alone reproduces `STREAMON`/`ret -3`. Evidence: `docs/test-reports/2026-07-26-gstreamer-explicit-caps.md` and `docs/test-reports/2026-07-26-milestone6-recorder-live.md`.
  - [x] **PI:** Add the production `dashcamd` entry point, systemd notify/watchdog/shutdown integration, and then enable/start the reviewed recorder unit without requiring a microphone. The unit reached `state=RECORDING`, remained watchdog-healthy with zero restarts, and completed both short and post-rollover stops with exit 0. Evidence: `docs/test-reports/2026-07-26-milestone6-recorder-live.md`.
  - [x] **PI:** Implement bounded queues and non-blocking segment finalization. The graph uses bounded recording/event queues and asynchronous split finalization; the exact stack's active-fragment closure is identity-validated because it omits pipeline EOS. Stale prior closures are rejected as shutdown completion. Evidence: `docs/test-reports/2026-07-26-milestone6-recorder-live.md`.
  - [x] **PI:** Rotate near 60 seconds on closed-GOP IDR boundaries without restarting the camera/encoder. The final accepted boundary produced a 59.988667-second 8,004,656-bit/s clip and an IDR-started successor while one service instance remained healthy and unthrottled. Evidence: `docs/test-reports/2026-07-26-milestone6-recorder-live.md`.
  - [x] **PI:** Write to `pending`, flush/finalize, collision-check, and rename to `clips`. The hash-closed exact-Pi release read back/promoted ordinary and clean-shutdown MP4+JSON pairs, refused a case-colliding target without pair mutation, and retained the earlier fail-closed diagnostic. Evidence: `docs/test-reports/2026-07-26-milestone6-finalization-live.md`.
  - [x] **PI:** Persist clip lifecycle and reconcile the ext4 index with exFAT after clean/unclean restarts. An injected post-intent one-member promotion interruption reconciled on normal `dashcamd` restart; final catalog state has three `FINALIZED` reconciled rows and zero pending intents. Evidence: `docs/test-reports/2026-07-26-milestone6-finalization-live.md`.
  - [x] **PI:** Expose effective settings, frames, drops, bitrate, clip timing, restart count, and storage preflight state. The installed status snapshot was truthfully sampled during the recovery/endurance evidence; see `docs/test-reports/2026-07-27-milestone6-metrics-recovery-endurance-live.md`.
  - [x] **PI:** Add bounded camera/encoder recovery with backoff and accurate `FAULTED`/`DEGRADED` reasons. One injected recoverable camera/encoder fault passed with the 1/2/4-second bounded policy, a fresh backend, and clean camera release; see `docs/test-reports/2026-07-27-milestone6-metrics-recovery-endurance-live.md`.
  - [x] **PI:** Validate 10 consecutive independently playable clips, IDR starts, duration, and normalized boundary continuity. Sequences 30–39 passed on the exact exFAT volume; see `docs/test-reports/2026-07-27-milestone6-metrics-recovery-endurance-live.md`.
  - [x] **PI:** Pass a two-hour video-only endurance test with stable memory and no sustained throttling. The retained 7,200-second result passed the strict hash-bound zram-only/no-growth reanalysis; see `docs/test-reports/2026-07-27-milestone6-metrics-recovery-endurance-live.md`.
  - [x] Exit gate: reliable 1080p30 hardware-encoded video-only segmentation passes Phase 1 evidence requirements. The recorder was stopped cleanly after the accepted run; see `docs/test-reports/2026-07-27-milestone6-metrics-recovery-endurance-live.md`.

## Milestone 7 — USB audio and A/V synchronization

- [x] **Milestone 7 complete** — Connected-microphone A/V and microphone-absent startup both passed on the exact Pi; evidence: `docs/test-reports/2026-07-27-milestone7-audio-live.md` and `docs/test-reports/2026-07-28-milestone7-absent-startup-live.md`.
  - [x] **LOCAL:** Implement bounded stable ALSA identity discovery, prevent volatile-index substitution, add the fixed AAC graph/status/sidecar contracts, and keep unmatched outcomes on the exact video-only graph. Evidence: `docs/test-reports/2026-07-27-milestone7-audio-live.md`.
  - [x] **PI:** Add mono 48 kHz AAC-LC at 128 kbit/s to the selected muxing pipeline. Ten ordinary clips passed exact caps, truthful sidecars, IDR, and independent A/V decode; evidence: `docs/test-reports/2026-07-27-milestone7-audio-live.md`.
  - [x] **PI:** Timestamp audio from the pipeline clock and add required resampling/drift correction. The graph explicitly disables the USB clock as pipeline provider, uses pipeline timestamps plus resampling, and passed the ten-clip skew gate; evidence: `docs/test-reports/2026-07-27-milestone7-audio-live.md`.
  - **PI REFUSAL NOTE (2026-07-27):** The hash-closed, non-mutating exact-Pi probe rejected direct release/re-request of a live asynchronous `splitmuxsink` `audio_%u` pad: there is no public pre-switch drain/closure/new-mux barrier and asynchronous finalization leaves a context race. It performed zero camera, encoder, request-pad, service, network, or exFAT mutations. Evidence: `docs/test-reports/2026-07-27-milestone7-hotplug-refusal.md`. The next unproven architecture is immutable complete recording generations with a two-slot IDR handoff; do not check a fault task until its exact-Pi experiment passes.
  - **PI CAPABILITY NOTE (2026-07-27):** Two unchanged hash-closed runs passed one programmatic A/V -> video-only -> restored-A/V sequence with three immutable complete generations, a continuous camera/hardware encoder, IDR-first clips, one-frame video continuity, hardware decode, restored skew below 100 ms, bounded closure, and no warnings/restarts/throttling. Evidence: `docs/test-reports/2026-07-27-milestone7-generation-handoff-live.md`. This selected the immutable-generation architecture; the later production qualification below proves live ingress reconstruction, bounded slot recycling, and repeated logical reconnect.
  - **PI PRODUCTION LOGICAL-LOSS NOTE (2026-07-27):** Release `0.1.0.dev0-2439b9fc544ffffc` passed its then-one-way A/V -> video-only loss isolation with no audio-loss override. Controlled deauthorization of the exact matched microphone produced two stable `NOT_FOUND` confirmations and an IDR-held immutable-generation handoff; the resulting ordered clips had audio `[true,false]`, both were IDR-first and hardware-decoded, encoder drops stayed `9 -> 9`, and pipeline restarts stayed zero. Runtime truthfully reported `UNAVAILABLE/microphone_loss_isolated`; the exact identity rematched after cleanup. Evidence: `docs/test-reports/2026-07-27-milestone7-production-loss-live.md` (authoritative ignored result `artifacts/pi-m7-20260727/m7-production-loss-result-9.json`, SHA-256 `8c113bc4bda8fcb1d5017fa63ad510b75ba7984ef8f88c0dc8810cb5485c2ffb`). This is controlled software deauthorization, not physical-unplug evidence. Its restoration limitation is superseded by the final qualification below; physical hotplug is no longer a current acceptance task.
  - **PI RESTORATION QUALIFICATION NOTE (2026-07-28):** Final hash-closed release `0.1.0.dev0-ce028ba96d40fb9d` passed the unchanged two-cycle production qualification. The fix reserves generation EOS atomically before output dispatch, rejects ambiguous/duplicate EOS, tolerates at most one proven late source EOS, and accepts only exact open-or-already-closed fragment ownership when reusing that boundary for video closure. Result 72 completed the bounded three-slot progression with five finalized clips whose audio truth was `[true,false,true,false,true]`; all were IDR-first and hardware-decoded, A/V stream-edge skews were 76.001, 71.958, and 84.291 ms, drops and pipeline restarts remained zero, and throttling remained `0x0`. The strict 100 ms bound was not weakened after earlier 124.666 ms and 100.666 ms refusals. Evidence: `docs/test-reports/2026-07-28-milestone7-production-restoration-live.md`; ignored result `artifacts/pi-m7-20260727/m7-production-restoration-result-72.json`, SHA-256 `e3f16644c6aac8bf847bbd12feeb931fa818b0320a1fa6eb1f9aed486fe2910b`. This proves controlled logical sysfs loss/reconnect. It is retained as implemented resilience evidence, not as a physical hotplug acceptance requirement.
  - [x] **PI:** Start the ordinary production recorder with the configured microphone physically absent; record and finalize truthful video-only media without rootfs fallback. Release `0.1.0.dev0-09a6dd3b374d3952` reached `RECORDING` with `UNAVAILABLE/not_found`, finalized an IDR-first hardware-decoded 59.989-second 1080p High/4.1 clip at 8.004 Mbit/s with `audio.available=false`, and stopped cleanly with zero restarts/throttling; evidence: `docs/test-reports/2026-07-28-milestone7-absent-startup-live.md`.
  - [x] **PI:** Restore audio at the earliest supported safe boundary and report per-clip availability accurately. Two controlled logical loss/reconnect cycles passed the real production recorder with bounded three-slot recycling and audio truth `[true,false,true,false,true]`; evidence: `docs/test-reports/2026-07-28-milestone7-production-restoration-live.md`. This is additional implemented resilience, not a physical hotplug acceptance requirement.
  - **OWNER SCOPE DECISION (2026-07-28):** The microphone is expected to remain connected in normal use. Physical hot-unplug/replug, repeated physical cycling, runtime restoration, naturally reassigned-index, and physical wrong-device qualification are not current v1 acceptance tasks. The dashcam must work with the configured microphone present or absent at recorder startup; stable identity and non-substitution remain required.
  - [x] **PI:** Verify per-clip A/V skew below 100 ms with no systematic growth in an endurance run. Ten consecutive clips measured 4.000–64.333 ms stream-edge skew with no upward drift and exact zero normalized boundary deltas; evidence: `docs/test-reports/2026-07-27-milestone7-audio-live.md`.
  - [x] Exit gate: A/V is synchronized when the configured microphone is present, and recorder startup without it produces truthful video-only recording.

## Milestone 8 — GPS, time, metadata, and filename reconciliation

- [x] **Milestone 8 complete**
  - [x] **PI:** Integrate the UART reader with bounded buffers, reconnect/backoff, and explicit GPS states. The exact Pi passed the final raw 115200 PL011 adapter with `VMIN=1`/`VTIME=0`, nonblocking receive-only access, bounded zero-byte/no-progress failure, and one recovered transport disconnect without a pipeline restart; evidence: `docs/test-reports/2026-07-28-milestone8-gps-uart-live.md`.
  - [x] **PI:** Validate supported NMEA talkers/sentences and checksum/error counters with the actual receiver. The 72-second production run reached `NAVIGATION_VALID`, latest GN GGA with nine satellites, and recorded bounded valid/checksum/unsupported counters; a subsequent privacy-safe 120-second comparator accepted 1,199 GGA and 1,200 active RMC records with 12 satellites, two checksum failures, and zero malformed records; evidence: `docs/test-reports/2026-07-28-milestone8-gps-uart-live.md`.
  - [x] **PI:** Start recording with no GPS and preserve boot ID, sequence, and monotonic clip/sample times. The exact release ran against deliberately absent `/dev/dashcam-gps-deliberately-absent` while the physical module remained connected; sequences 383/384 retained the exact boot/sequence and monotonic boundary, nullable UTC/local fields, `UNSYNCED`/`MONOTONIC_ONLY`, and `gps.available=false` through nine bounded unavailable attempts, with zero service/pipeline restarts; evidence: `docs/test-reports/2026-07-28-milestone8-no-gps-live.md`.
  - [x] **PI:** Accept trusted GPS UTC anchors using sentence-specific validity, date plausibility, continuity, source, and uncertainty. Release `0.1.0.dev0-75947a15db03f4b3` accepted one source-qualified GN RMC anchor and 199 continuity confirmations on the exact Pi with configured plausibility/conflict bounds, explicit 250 ms uncertainty, zero anchor rejection, and no media restart; evidence: `docs/test-reports/2026-07-28-milestone8-gps-anchor-live.md`.
  - [x] **PI:** Configure exactly one system-clock owner and prove a clock step does not alter media PTS/DTS. The approved image has only `systemd-timesyncd` active for Linux wall time; a controlled +120-second realtime step during sequence 390 left all video/audio PTS and DTS strictly increasing, preserved full hardware decode, caused no media/service restart, and ended with the owner active and synchronized; evidence: `docs/test-reports/2026-07-28-milestone8-clock-step-live.md`.
  - [x] **LOCAL/PI:** Generate one bounded, versioned, atomic JSON sidecar per finalized clip. Release `0.1.0.dev0-7fd1e73debb731b6` retained the receiver's native cadence as exactly 600 unique, ordered, monotonic-only GPS samples in a full clip and 431 in its shutdown successor, with half-open boundary ownership, canonical sidecar readback, zero sample overlap, and no media restart; evidence: `docs/test-reports/2026-07-28-milestone8-gps-sidecar-live.md`.
  - [x] **LOCAL/PI:** Keep UTC/local fields nullable while unsynced, then reconcile sidecars and filenames idempotently after a trusted anchor. Release `0.1.0.dev0-6f943f3a4edf7117` finalized provisional no-GPS sequences 398/399, later projected both from one trusted RMC anchor, durably renamed both pairs, drained its bounded backlog `1 -> 0`, and preserved truthful empty historical navigation; evidence: `docs/test-reports/2026-07-28-milestone8-reconciliation-live.md`.
  - [x] **LOCAL/PI:** Guarantee filename collision refusal and stable UUID API identity across reconciliation. Startup replay preserved sequences 393/394 UUIDs, live late-lock preserved sequences 398/399 UUIDs, and an isolated exact-exFAT case-variant collision refused before source/catalog/intent mutation while leaving the production catalog hash unchanged; evidence: `docs/test-reports/2026-07-28-milestone8-reconciliation-live.md`.
  - [x] **PI:** Mark stale/lost GPS without repeating coordinates or speed as current. Exact-Pi production-wheel validation cleared navigation on silence and transport loss, retained only the bounded UTC anchor as `GPS_TIME_STALE`, and restored `NAVIGATION_VALID` only after a new valid fix. The integrated production-daemon run advanced 1 -> 2,179 encoded frames with zero drops/restarts while exercising the same states; privacy-safe result SHA-256 `a89df67c2c9735127bf360718738f9d1b09049bcb429034e25793b1f2be17ffd`; evidence: `docs/test-reports/2026-07-28-milestone8-reconciliation-live.md`.
  - [x] **PI:** Test no-GPS boot, late lock, loss, reconnect, malformed input, implausible date, anchor conflict, UTC midnight, and DST transitions. A controlled boot into deliberately absent configured GPS reached truthful `UART_UNAVAILABLE`/`UNSYNCED` recording with 2,106 encoded frames and zero drops/restarts before exact config restoration. Late lock passed with the physical receiver path; the bounded policy matrix passed against the installed production wheel, and the integrated PTY run proved real camera/encoder/exFAT/catalog continuity through silence, conflict, loss, and reconnect; evidence: `docs/test-reports/2026-07-28-milestone8-no-gps-live.md` and `docs/test-reports/2026-07-28-milestone8-reconciliation-live.md`.
  - [x] Exit gate: metadata remains internally consistent through unsynced startup and GPS/time faults without disturbing media capture. Final hash-closed release `0.1.0.dev0-921164f96ad53e0b` passed exact-version/idempotent installation, the full local suite (`1845 passed, 10 skipped`), an ordinary-recorder one-minute media run with zero pipeline/service restart, independent hardware-H.264 plus AAC decode, and the integrated fault matrix with zero dropped frames; evidence: `docs/test-reports/2026-07-28-milestone8-reconciliation-live.md`.

## Milestone 9 — Burned-in overlay

- [ ] **Milestone 9 complete**
  - [x] **LOCAL:** Define overlay field formatting, numeric UTC offset, units, stale/invalid states, and layout bounds. Evidence: `docs/test-reports/2026-07-24-pre-pi-implementation.md`.
  - [x] **PI:** Implement the measured fixed-region/pre-rendered overlay path before the recording encoder. The original `textoverlay` candidate proved unsafe at about 10.4 fps and stock `gdkpixbufoverlay` at about 18.3 fps. The recorder-owned native NV12/DMABUF path subsequently recorded active GPS/audio/overlay, hardware 1080p30, and verified exFAT with 2,103 frames and zero drops/restarts/renderer failures in the exact v7 wheel. That wheel remains installed as accepted dormant hash-closed release `0.1.0.dev0-5f95dd806342ac9e`. A same-boot fused-validation candidate from Git `a9871cf` lowered one 75-second probe's mean CPU but worsened p95 by 0.9989 percentage points and had one pre-window encoder-input-PTS-gap drop, so it was rejected and rolled back; its source remains rejected evidence in Git. Resource acceptance remains unchecked. Evidence: `docs/test-reports/2026-08-03-milestone9-overlay-resource-limit.md` and `docs/test-reports/2026-08-09-milestone9-fused-validation-rejected.md`.
  - [ ] **PI:** Show `TIME UNSYNCED`, `GPS LOST`, and hidden/marked stale navigation values correctly.
  - [ ] **PI:** Prove the overlay and sidecar use the same telemetry snapshot/time model.
  - [ ] **PI:** Measure paired no-overlay versus overlay arms using the prespecified Section C1 gate: one warm-up plus at least ten one-minute clips per arm, at least 1 Hz resource samples, every clip at least 29.9 fps, zero drop/restart increase, mean CPU delta at most 35 percentage points and overlay p95 at most 100%, mean RSS delta at most 16 MiB and growth at most 32 MiB, no non-zram swap and at most 4 MiB within-arm zram growth, zero throttle/undervoltage, and temperature at most 80 C. The earlier v7 active-GPS/audio/overlay diagnostic failed p95 at 100.9876%; the later fused-validation candidate was rejected before its second screening run because p95 regressed by 0.9989 percentage points instead of improving by the required 2 points and it had a pre-window drop. Neither is the required ten-clip paired matrix. Evidence: `docs/test-reports/2026-08-03-milestone9-overlay-resource-limit.md` and `docs/test-reports/2026-08-09-milestone9-fused-validation-rejected.md`.
  - [ ] **PI:** Run clip-boundary, GPS-loss/recovery, and changing-text stress tests.
  - [x] **PI:** If the overlay misses the 1080p30 gate, stop and document evidence/options; do not silently lower the product profile. The `textoverlay` and `gdkpixbufoverlay` candidates were rejected without changing the profile. The native NV12/DMABUF implementation preserves 1080p30 and zero drops/restarts, but its repeated diagnostic p95 CPU slightly exceeds the strict resource ceiling; the remaining choices are further evidence-backed optimization or an explicit product-contract decision, never silent acceptance. Evidence: `docs/test-reports/2026-08-03-milestone9-overlay-candidate-failure.md` and `docs/test-reports/2026-08-03-milestone9-overlay-resource-limit.md`.
  - [ ] Exit gate: the burned-in overlay sustains the default recording profile with no statistically meaningful recording regression.

## Milestone 10 — Retention, protection, and crash reconciliation

- [ ] **Milestone 10 complete**
  - [x] **LOCAL:** Implement the durable clip catalog/migrations on ext4 and startup reconciliation against exFAT. Evidence: `docs/test-reports/2026-07-24-pre-pi-implementation.md`.
  - [ ] **LOCAL/PI:** Implement exact low/high/emergency free-space threshold behavior.
  - [ ] **LOCAL/PI:** Implement oldest-first retention using durable `DELETING` intent and idempotent two-file cleanup.
  - [ ] **LOCAL/PI:** Implement bounded download leases and expiry after crashed/abandoned clients.
  - [ ] **LOCAL/PI:** Implement event protection for previous 2, current, and next 1 clips with catalog/retention coordination.
  - [ ] **LOCAL/PI:** Implement protect/unprotect moves as recoverable two-file operations.
  - [ ] **PI:** Verify unknown/Windows-created files are ignored and preserved.
  - [ ] **PI:** Inject interruption after every finalize/protect/unprotect/delete step and verify deterministic reconciliation.
  - [ ] **PI:** Fill a small test filesystem, cycle retention repeatedly, and prove active/finalizing/protected/leased clips are never selected.
  - [ ] **PI:** Fill the volume with protected clips and verify explicit critical behavior before evidence destruction.
  - [ ] **PI:** Prove retention/reconciliation do not create recording gaps or unbounded startup delay.
  - [ ] Exit gate: ring retention is race-safe and crash-recoverable within the documented exFAT limitations.

## Milestone 11 — Access point, web UI, preview, and controlled shutdown

- [ ] **Milestone 11 complete**
  - [ ] **PI:** Configure the NetworkManager AP, DHCP, unique WPA passphrase, fixed direct IP, and optional mDNS.
  - [ ] **PI:** Prove AP/DHCP/mDNS failure cannot stop or restart recording.
  - [ ] **LOCAL/PI:** Implement the unprivileged web service and versioned API over the restricted recorder socket.
  - [ ] **LOCAL/PI:** Add authenticated sessions, CSRF protection, input allow-lists, secret redaction, and rate/size limits.
  - [ ] **LOCAL/PI:** Implement status, settings, clip list/detail, protected filters, event, restart, and structured errors.
  - [ ] **LOCAL/PI:** Implement controlled downloads through recorder-approved clip IDs and bounded leases; reject traversal and stale IDs.
  - [ ] **PI:** Benchmark preview candidates on the reference phone and select one from measured results.
  - [ ] **PI:** Implement on-demand preview with bounded/leaky queues, one client by default, and no second camera owner.
  - [ ] **PI:** Meet median preview latency below 500 ms and 95th percentile below 1 second without recording regression.
  - [ ] **LOCAL/PI:** Implement the confirmed/re-authenticated prepare-removal flow through a narrow privileged helper.
  - [ ] **PI:** Verify the flow rejects new operations, finalizes with a timeout, flushes, stops writers, unmounts, reports shutdown-in-progress, and powers down.
  - [ ] **PI:** Document and validate the physical ACT-LED/power-controller cue for safe card removal.
  - [ ] **PI:** Restart/crash the web service and connect slow/repeated clients without camera or recorder restart.
  - [ ] Exit gate: the complete phone workflow is secure, bounded, and operationally independent from recording.

## Milestone 12 — Release image, interoperability, and hardening

- [ ] **Milestone 12 complete**
  - [ ] **PI:** Produce the repeatable release image/customization process with exact versions and checksums.
  - [ ] **PI:** Verify fresh-install, upgrade, migration, uninstall/recovery, and second-boot idempotency procedures.
  - [ ] **WIN:** After controlled shutdown, validate `DASHCAM` visibility, MP4 seek/playback, JSON readability, and pair copying on Windows 10.
  - [ ] **WIN:** Repeat the interoperability validation on Windows 11 and representative multi-partition card readers.
  - [ ] **WIN/PI:** Reinsert a Windows-used card and prove Windows metadata is preserved/ignored and recording resumes without formatting.
  - [ ] **PI:** Run the full 12-hour endurance test with GPS, microphone, AP, periodic preview, and at least two retention cycles.
  - [ ] **PI/DESTRUCTIVE:** Run the declared randomized abrupt-power matrix on named reference card models and preserve every result/log.
  - [ ] **PI:** Run camera, encoder, storage, UART, thermal, undervoltage, AP, web, and microphone fault injection.
  - [ ] **PI:** Verify bounded recovery time, durable catalog usability, no rootfs fallback, and no automatic exFAT formatting.
  - [ ] **PI:** Record CPU, RSS/memory, swap, temperature, throttling, undervoltage, drops, bitrate, A/V skew, preview latency, and storage throughput.
  - [ ] **LOCAL:** Complete installation, configuration, hardware, architecture, Windows, troubleshooting, backup, and recovery documentation.
  - [ ] **LOCAL:** Map every requirement and acceptance test in `Pizero_dashcam_PROJECT.md` to a passing evidence artifact or documented accepted deviation.
  - [ ] **LOCAL:** Run final formatting, lint, type, unit, integration, security, packaging, and documentation checks.
  - [ ] Exit gate: every definition-of-done item has evidence, all deviations are explicitly accepted, and the release artifact is reproducible.
